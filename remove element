class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        i=0
        while i in range(len(nums)):
            if val==nums[i]:
                nums.pop(i)
            else:
                i+=1    
